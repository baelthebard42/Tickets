import { CustomError } from "./custom-errors";
export declare class NotAuthorizedError extends CustomError {
    constructor();
    statusCode: number;
    serializeErrors(): {
        message: string;
    }[];
}
